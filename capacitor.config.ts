import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.heart.heartapp',
  appName: 'The Heart',
  webDir: 'build',
  bundledWebRuntime: false,
  // ios: {
  //   "preferredContentMode": "mobile"
  // },
};

export default config;
